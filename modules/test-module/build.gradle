plugins {
    id 'java'
}

group 'dev.redicloud.module.test'

dependencies {
    compileOnly project(':module-handler')
    compileOnly project(':api:api')
}

jar {
    archiveFileName.set("${project.name}.jar")
}